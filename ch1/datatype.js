let a = 221;
let b = a - 4;
console.log(b);

const c = 221;

// var은 문제가 발생할 수 있음

const what = "Jeongwon";

// 변수의 타입이라는 것을 선언부에 작성하지 않는듯.

const daysOfWeek = ["Mon", "Tue", "Wed", "Thu", "Fri"];
console.log(daysOfWeek);

// 같은 어레이에 다른 데이타 타입을 넣을 수 있음. 

const nicoInfo = {
    name:"Nicolas",
    age:55,
    gender:"Male",
    handsome:true,
    favFood: [
        {name:"Kimchi",fatty:false},
        {name:"Cheese Burger", fatty:true}]
} // Object임

console.log(nicoInfo,console); 